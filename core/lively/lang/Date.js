///////////////////////////////////////////////////////////////////////////////
// Extensions to Date instances
///////////////////////////////////////////////////////////////////////////////

Object.extend(Date, {
    ensureTimeStamp: function(obj) {
        return obj.timeStamp = Date.now();
    },
});